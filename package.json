{
  "name": "reactuse",
  "version": "0.0.90",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint-staged": "lint-staged",
    "unit-test": "pnpm --filter @siberiacancode/reactuse run unit-test",
    "prepublishOnly": "pnpm type && pnpm unit-test run && pnpm build",
    "build": "shx rm -rf dist && rollup -c --bundleConfigAsCjs",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3"
  },
  "lint-staged": {
    "*.js": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.ts": [
      "eslint --fix",
      "prettier --write"
    ]
  }
}
